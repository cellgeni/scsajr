% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{load_sc_as}
\alias{load_sc_as}
\title{Load single-cell AS counts (inclusion/exclusion) as sparse matrices}
\usage{
load_sc_as(segs, path)
}
\arguments{
\item{segs}{A data.frame or `SummarizedExperiment`` whose row names are the segment IDs
to load. Only those segment IDs will be returned in the output matrices.}

\item{path}{Character; file prefix (without extension) for inclusion/exclusion data.
The function expects files:
\itemize{
\item \code{paste0(path, ".i.mtx")} (or \code{.i.mtx.gz}) and accompanying keys
for inclusion counts, and
\item \code{paste0(path, ".e.mtx")} (or \code{.e.mtx.gz}) and accompanying keys
for exclusion counts.
Uses `read_named_mm()`` to read each.
}}
}
\value{
A list with two sparse matrices of class \code{dgCMatrix}:
\itemize{
\item \code{i}: inclusion counts (rows = \code{rownames(segs)}, columns as in the \code{.i} matrix),
\item \code{e}: exclusion counts (rows = \code{rownames(segs)}, columns matching the inclusion matrix).
}
}
\description{
Given a set of segment IDs and a file prefix, this function reads inclusion (\code{.i.mtx})
and exclusion (\code{.e.mtx}) counts via \code{read_named_mm()},
then subsets to only the requested segments and ensures consistent columns between \code{i} and \code{e}.
}
\details{
\enumerate{
\item Calls `read_named_mm(paste0(path, ".e"))`` to load the exclusion count matrix.
\item Calls `read_named_mm(paste0(path, ".i"))`` to load the inclusion count matrix.
\item Subsets both matrices to only rows matching `rownames(segs)``.
\item Ensures that the inclusion matrix uses the same column ordering as the exclusion matrix.
\item Returns a list `list(e = e_sub, i = i_sub)``.
}
}
\seealso{
\code{\link{read_named_mm}}, \code{\link{load_introns_as_se}}
}
