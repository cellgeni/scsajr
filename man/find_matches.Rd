% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{find_matches}
\alias{find_matches}
\title{Find matches or reverse-complement matches of a pattern in a sequence}
\usage{
find_matches(seq, pattern, ignore_case = TRUE, do_reverse = TRUE, ...)
}
\arguments{
\item{seq}{A single character string representing a DNA sequence.}

\item{pattern}{A character string or regular expression to match.}

\item{ignore_case}{Logical; if \code{TRUE}, matching is case-insensitive. Default is \code{TRUE}.}

\item{do_reverse}{Logical; if \code{TRUE}, also search for reverse complement of \code{pattern}. Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{gregexpr()}, such as \code{fixed = TRUE}.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{from}: start position of each match (1-based),
\item \code{to}: end position of each match,
\item \code{dir}: \code{"f"} for forward matches, \code{"r"} for reverse complement matches.
If no matches are found, returns \code{NULL}.
}
}
\description{
Searches for a given pattern in a DNA sequence, optionally also searching for its
reverse complement. Returns a data.frame of match positions and strand information.
}
\details{
\enumerate{
\item Uses \code{gregexpr(pattern, seq, ignore_case = ignore_case, ...)} to find all forward matches.
If \code{gregexpr} returns \code{-1}, no forward matches exist.
\item If \code{do_reverse = TRUE}, computes the reverse complement of \code{pattern} via \code{rcomp(pattern)} and searches again for reverse matches.
\item Combines forward and reverse matches into a single data.frame, sets column names, and returns.
}
}
\examples{
\dontrun{
seq <- "ATGCGTATGC"
hits <- find_matches(seq, "ATG")
print(hits)
# Might return positions where "ATG" (forward) or "CAT" (reverse complement) occur
}

}
\seealso{
\code{\link{rcomp}}
}
