% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{get_dit_plot_size}
\alias{get_dit_plot_size}
\title{Compute recommended plot dimensions for a compareCluster result}
\usage{
get_dit_plot_size(ccr)
}
\arguments{
\item{ccr}{A \code{compareClusterResult} object (from \code{clusterProfiler::compareCluster()}), or \code{NULL}.}
}
\value{
A numeric vector of length 2:
\itemize{
\item \code{height}: if \code{ccr} is \code{NULL}, \code{2}; otherwise \code{sum(pmin(5, size)) * 0.15 + 3},
where \code{size} is the number of terms per cluster in \code{ccr@compareClusterResult}.
\item \code{width}: if \code{ccr} is \code{NULL}, \code{2}; otherwise \code{length(size) * 0.6 + 7},
where \code{length(size)} is the number of clusters with at least one term.
}
}
\description{
Given a \code{compareClusterResult} object from \code{clusterProfiler},
this function returns a two‐element numeric vector giving height and width for plotting the results.
The height scales with the number of clusters (up to 5 rows per cluster),
and the width scales with the number of clusters along the x‐axis.
If \code{ccr} is \code{NULL}, returns \code{c(2, 2)}.
}
\details{
\enumerate{
\item If \code{ccr} is \code{NULL}, returns \code{c(2, 2)}.
\item Otherwise:
\itemize{
\item Extract \code{ccr@compareClusterResult$Cluster} to tabulate how many terms (rows) belong to each cluster.
\item Cap each cluster’s row count at 5 via \code{pmin(5, size)}.
\item Compute:
\code{height = sum(pmin(5, size)) * 0.15 + 3},
\code{width  = length(size) * 0.6 + 7}, where \code{size} excludes clusters with zero terms.
}
}
}
\examples{
\dontrun{
dims1 <- get_dit_plot_size(ccr) # for a valid compareClusterResult
dims2 <- get_dit_plot_size(NULL) # returns c(2, 2)
}

}
\seealso{
\code{\link{dotplot}}
}
