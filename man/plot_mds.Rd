% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{plot_mds}
\alias{plot_mds}
\title{Plot MDS with group connections and labels}
\usage{
plot_mds(xy, celltypes, ct2col, samples, ...)
}
\arguments{
\item{xy}{A numeric matrix or data.frame with two columns (dimensions) and rows corresponding to samples.
Row names should match \code{samples}.}

\item{celltypes}{A character vector giving the group label for each row of \code{xy}.
Length must equal \code{nrow(xy)}.}

\item{ct2col}{A named vector mapping each unique group label in \code{celltypes} to a plotting color.}

\item{samples}{A vector (or factor) of sample identifiers (same order as rows of \code{xy}),
used to choose point characters. Can be numeric or character; coerced to factor internally.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics]{plot}()}.}
}
\value{
Invisibly returns \code{NULL}; plots a scatter of \code{xy} points with colored segments and labels.
}
\description{
Given two-dimensional coordinates for samples (e.g., output of \code{cmdscale()}),
this function plots each point colored by its group,
draws segments connecting all pairs of points within the same group,
and labels each group at the mean of its points.
}
\details{
\enumerate{
\item Each point \code{xy[i, ]} is plotted using \code{pch = as.numeric(factor(samples[i])) \%\% 25}
and color \code{ct2col[celltypes[i]]}.
\item For each distinct group \code{ct}, all pairs of points \code{xy[which(celltypes == ct), ]} are
connected with line segments of color \code{ct2col[ct]}.
\item At the mean coordinate of each group’s points, the group label \code{ct} is drawn.
}
}
\examples{
\dontrun{
# Suppose 'mds_coords' is a matrix of size (n_samples × 2),
# 'groups' is a factor of length n_samples, and 'colors' maps groups to colors.
plot_mds(mds_coords, groups, colors, samples = rownames(mds_coords))
}

}
\seealso{
\code{\link{cmdscale}}, \code{\link{plot}}
}
