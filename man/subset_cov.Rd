% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{subset_cov}
\alias{subset_cov}
\title{Subset a coverage object to a specified genomic window}
\usage{
subset_cov(cov, start, stop)
}
\arguments{
\item{cov}{A coverage object (list) containing at least:
\itemize{
\item \code{x}: numeric vector of genomic positions,
\item \code{cov}: numeric vector or Rle of coverage values at those positions,
\item \code{start}, \code{end}: numeric scalars indicating the current coverage bounds,
\item \code{juncs}: data.frame of junctions with numeric columns \code{start} and \code{end}.
}}

\item{start}{Numeric; new window start coordinate.}

\item{stop}{Numeric; new window stop coordinate.}
}
\value{
The same coverage object \code{cov}, but with:
\itemize{
\item \code{cov$cov} and \code{cov$x} filtered to positions \code{x >= start & x <= stop},
\item \code{cov$start} set to \code{start}, \code{cov$end} set to \code{stop},
\item \code{cov$juncs} filtered to rows where \code{(juncs$start <= stop & juncs$end >= start)}.
}
}
\description{
Given a coverage object (a list with components \code{x}, \code{cov}, \code{start}, \code{end}, and \code{juncs}),
this function filters the coverage and junctions to only include data within \verb{[start, stop]}.
}
\examples{
\dontrun{
# Given cov with positions 1:1000 and junctions spanning various ranges:
cov_window <- subset_cov(cov, start = 100, stop = 200)
}

}
