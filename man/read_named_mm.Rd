% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{read_named_mm}
\alias{read_named_mm}
\title{Read a Matrix Market file with accompanying row/column keys}
\usage{
read_named_mm(prefix)
}
\arguments{
\item{prefix}{Character; file path prefix for the Matrix Market data.
If \code{prefix.mtx} or \code{prefix.mtx.gz} exists, reads it via \code{Matrix::readMM()}.
Row names are read from \code{prefix_key1.csv(.gz)}, column names from \code{prefix_key2.csv(.gz)}.}
}
\value{
A sparse matrix (\code{dgCMatrix}) with row names set to feature IDs (from key1) and
column names set to barcodes (from key2). If no \code{.mtx} file is found, returns \code{NULL}.
}
\description{
Attempts to load a sparse matrix from \verb{<prefix>.mtx} or \verb{<prefix>.mtx.gz},
and then assigns row names from \verb{<prefix>_key1.csv} (or \code{.gz}) and column names from \verb{<prefix>_key2.csv} (or \code{.gz}).
}
\examples{
\dontrun{
# Suppose files "data/counts.mtx" and "data/counts_key1.csv", "data/counts_key2.csv" exist:
mat <- read_named_mm("data/counts")
dim(mat)
head(rownames(mat))
head(colnames(mat))
}

}
\seealso{
\code{\link[Matrix]{readMM}}, \code{\link{load_introns_as_se}}
}
