% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{calc_cpm}
\alias{calc_cpm}
\title{Calculate counts per million (CPM) per gene/feature}
\usage{
calc_cpm(se)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with an assay named \code{counts} (integer or numeric matrix).
Rows are features (e.g., genes); columns are samples.}
}
\value{
A numeric matrix of the same dimensions as the \code{counts} assay, where each entry is the counts per million for that feature and sample.
If \code{counts} assay is missing, returns \code{NULL}.
}
\description{
Given a \code{SummarizedExperiment} containing raw count data (assay named \code{counts}),
this function computes counts per million for each feature across samples: \code{CPM = (counts / library_size) * 1e6}
where \code{library_size} is the total sum of counts in each sample (column).
}
\examples{
\dontrun{
# Assume 'se_counts' is a SummarizedExperiment with assay 'counts'
cpm_mat <- calc_cpm(se_counts)
head(cpm_mat)
}

}
\seealso{
\code{\link{pseudobulk}}
}
