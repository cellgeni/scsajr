% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{load_introns_as_se}
\alias{load_introns_as_se}
\title{Load intron counts as a SummarizedExperiment}
\usage{
load_introns_as_se(path)
}
\arguments{
\item{path}{Character; file prefix for the Matrix Market data.
If \code{path.mtx} or \code{path.mtx.gz} exists, \code{read_named_MM(path)} is used to load the sparse matrix.}
}
\value{
A \code{SummarizedExperiment} with:
\itemize{
\item An assay named \code{counts} (a sparse \code{dgCMatrix}) storing intron counts: rows = intron IDs, columns = barcodes.
\item \code{rowRanges}: a \code{GRanges} built by parsing row names (intron IDs) of the form \code{chr:strand:start-end}.
It sets \code{seqnames = chr}, \code{ranges = IRanges(start, end)}, and \code{strand} as \code{"+"} or \code{"-"}.
\item \code{colData}: a data.frame with one column \code{barcode} (the column names of the count matrix).
If no \code{.mtx} file is found, returns \code{NULL}.
}
}
\description{
Reads a Matrix Market file (and accompanying keys) to build a \code{SummarizedExperiment}
where rows are intron segments and columns are cell barcodes.
Uses \code{read_named_MM()} to load the counts matrix along with row and column names.
}
\seealso{
\code{\link{read_named_MM}}, \code{\link{SummarizedExperiment}}, \code{\link[Matrix]{readMM}}
}
