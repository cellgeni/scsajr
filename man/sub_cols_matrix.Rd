% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scsajr.R
\name{sub_cols_matrix}
\alias{sub_cols_matrix}
\title{Subset or reorder sparse matrix columns to a specified set}
\usage{
sub_cols_matrix(mat, target_cols)
}
\arguments{
\item{mat}{A matrix or sparse matrix with named columns.}

\item{target_cols}{Character vector specifying the desired column names (and order).}
}
\value{
A sparse matrix (\code{dgCMatrix}) with columns exactly \code{target_cols}. Any column in
\code{target_cols} not originally present in \code{mat} will be inserted as a zero‐filled sparse column.
Row names are unchanged.
}
\description{
Given a matrix or sparse matrix and a target set of column names, this function returns
a new sparse matrix whose columns match exactly the target set. Existing columns are
retained (and reordered if needed), and any target column not present is added as a
zero‐filled sparse column.
}
\examples{
\dontrun{
library(Matrix)
m <- sparseMatrix(
  i = c(1, 2, 1), j = c(1, 2, 3), x = c(1, 2, 3),
  dims = c(2, 4), dimnames = list(c("r1", "r2"), c("A", "B", "D", "E"))
)
result <- sub_cols_matrix(m, c("A", "B", "C", "D", "E"))
# 'C' inserted as sparse zero column; columns ordered A,B,C,D,E
}

}
